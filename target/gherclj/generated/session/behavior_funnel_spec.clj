(ns session.behavior-funnel-spec
  (:require [speclj.core :refer :all]
            [gherclj.core :as g]
            [gherclj.lifecycle :as lifecycle]
            [isaac.foundation.root-steps :as root-steps]
            [isaac.session.session-steps :as session-steps]))

(describe "Canonical session-behavior resolution funnel"

  (around [it]
    (binding [g/*state* (atom {})]
      (lifecycle/run-before-feature-hooks!)
      (try
        (it)
        (finally
          (lifecycle/run-after-feature-hooks!)))))

  (around [it]
    (binding [g/*state* (atom @g/*state*)]
      (lifecycle/run-before-scenario-hooks!)
      (try
        (it)
        (finally
          (lifecycle/run-after-scenario-hooks!)))))

  (it "Cascade precedence resolves the right <field> at creation — history-retention, , , , , , :retain"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.history-retention" is ""
    ;; and the isaac config path "crew.main.history-retention" is ""
    ;; and the isaac config path "models.spark.history-retention" is ""
    ;; and the isaac config path "providers.test.history-retention" is ""
    ;; when a session "s" is created with explicit history-retention ""
    ;; then the resolved behavior for "s" has history-retention ":retain"
    (pending "not yet implemented"))

  (it "Cascade precedence resolves the right <field> at creation — history-retention, , , , , :prune, :prune"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.history-retention" is ":prune"
    ;; and the isaac config path "crew.main.history-retention" is ""
    ;; and the isaac config path "models.spark.history-retention" is ""
    ;; and the isaac config path "providers.test.history-retention" is ""
    ;; when a session "s" is created with explicit history-retention ""
    ;; then the resolved behavior for "s" has history-retention ":prune"
    (pending "not yet implemented"))

  (it "Cascade precedence resolves the right <field> at creation — history-retention, , , , :retain, :prune, :retain"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.history-retention" is ":prune"
    ;; and the isaac config path "crew.main.history-retention" is ""
    ;; and the isaac config path "models.spark.history-retention" is ""
    ;; and the isaac config path "providers.test.history-retention" is ":retain"
    ;; when a session "s" is created with explicit history-retention ""
    ;; then the resolved behavior for "s" has history-retention ":retain"
    (pending "not yet implemented"))

  (it "Cascade precedence resolves the right <field> at creation — history-retention, , , :prune, :retain, :retain, :prune"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.history-retention" is ":retain"
    ;; and the isaac config path "crew.main.history-retention" is ""
    ;; and the isaac config path "models.spark.history-retention" is ":prune"
    ;; and the isaac config path "providers.test.history-retention" is ":retain"
    ;; when a session "s" is created with explicit history-retention ""
    ;; then the resolved behavior for "s" has history-retention ":prune"
    (pending "not yet implemented"))

  (it "Cascade precedence resolves the right <field> at creation — history-retention, , :retain, :prune, :prune, :prune, :retain"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.history-retention" is ":prune"
    ;; and the isaac config path "crew.main.history-retention" is ":retain"
    ;; and the isaac config path "models.spark.history-retention" is ":prune"
    ;; and the isaac config path "providers.test.history-retention" is ":prune"
    ;; when a session "s" is created with explicit history-retention ""
    ;; then the resolved behavior for "s" has history-retention ":retain"
    (pending "not yet implemented"))

  (it "Cascade precedence resolves the right <field> at creation — history-retention, :prune, :retain, :retain, :retain, :retain, :prune"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.history-retention" is ":retain"
    ;; and the isaac config path "crew.main.history-retention" is ":retain"
    ;; and the isaac config path "models.spark.history-retention" is ":retain"
    ;; and the isaac config path "providers.test.history-retention" is ":retain"
    ;; when a session "s" is created with explicit history-retention ":prune"
    ;; then the resolved behavior for "s" has history-retention ":prune"
    (pending "not yet implemented"))

  (it "Cascade precedence resolves the right <field> at creation — effort, , , , , , 7"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.effort" is ""
    ;; and the isaac config path "crew.main.effort" is ""
    ;; and the isaac config path "models.spark.effort" is ""
    ;; and the isaac config path "providers.test.effort" is ""
    ;; when a session "s" is created with explicit effort ""
    ;; then the resolved behavior for "s" has effort "7"
    (pending "not yet implemented"))

  (it "Cascade precedence resolves the right <field> at creation — effort, , , , , 5, 5"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.effort" is "5"
    ;; and the isaac config path "crew.main.effort" is ""
    ;; and the isaac config path "models.spark.effort" is ""
    ;; and the isaac config path "providers.test.effort" is ""
    ;; when a session "s" is created with explicit effort ""
    ;; then the resolved behavior for "s" has effort "5"
    (pending "not yet implemented"))

  (it "Cascade precedence resolves the right <field> at creation — effort, , , , 6, 5, 6"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.effort" is "5"
    ;; and the isaac config path "crew.main.effort" is ""
    ;; and the isaac config path "models.spark.effort" is ""
    ;; and the isaac config path "providers.test.effort" is "6"
    ;; when a session "s" is created with explicit effort ""
    ;; then the resolved behavior for "s" has effort "6"
    (pending "not yet implemented"))

  (it "Cascade precedence resolves the right <field> at creation — effort, , , 7, 6, 5, 7"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.effort" is "5"
    ;; and the isaac config path "crew.main.effort" is ""
    ;; and the isaac config path "models.spark.effort" is "7"
    ;; and the isaac config path "providers.test.effort" is "6"
    ;; when a session "s" is created with explicit effort ""
    ;; then the resolved behavior for "s" has effort "7"
    (pending "not yet implemented"))

  (it "Cascade precedence resolves the right <field> at creation — effort, , 8, 7, 6, 5, 8"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.effort" is "5"
    ;; and the isaac config path "crew.main.effort" is "8"
    ;; and the isaac config path "models.spark.effort" is "7"
    ;; and the isaac config path "providers.test.effort" is "6"
    ;; when a session "s" is created with explicit effort ""
    ;; then the resolved behavior for "s" has effort "8"
    (pending "not yet implemented"))

  (it "Cascade precedence resolves the right <field> at creation — effort, 9, 8, 7, 6, 5, 9"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.effort" is "5"
    ;; and the isaac config path "crew.main.effort" is "8"
    ;; and the isaac config path "models.spark.effort" is "7"
    ;; and the isaac config path "providers.test.effort" is "6"
    ;; when a session "s" is created with explicit effort "9"
    ;; then the resolved behavior for "s" has effort "9"
    (pending "not yet implemented"))

  (it "Cascade precedence resolves the right <field> at creation — model, , , , , spark, spark"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "crew.main.model" is ""
    ;; and the isaac config path "models.spark.model" is ""
    ;; and the isaac config path "providers.test.model" is ""
    ;; when a session "s" is created with explicit model ""
    ;; then the resolved behavior for "s" has model "spark"
    (pending "not yet implemented"))

  (it "Cascade precedence resolves the right <field> at creation — model, , parrot, , , spark, parrot"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "crew.main.model" is "parrot"
    ;; and the isaac config path "models.spark.model" is ""
    ;; and the isaac config path "providers.test.model" is ""
    ;; when a session "s" is created with explicit model ""
    ;; then the resolved behavior for "s" has model "parrot"
    (pending "not yet implemented"))

  (it "Cascade precedence resolves the right <field> at creation — model, swift, parrot, , , spark, swift"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "crew.main.model" is "parrot"
    ;; and the isaac config path "models.spark.model" is ""
    ;; and the isaac config path "providers.test.model" is ""
    ;; when a session "s" is created with explicit model "swift"
    ;; then the resolved behavior for "s" has model "swift"
    (pending "not yet implemented"))

  (it "Cascade precedence resolves the right <field> at creation — crew, , , , , , main"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.crew" is ""
    ;; and the isaac config path "crew.main.crew" is ""
    ;; and the isaac config path "models.spark.crew" is ""
    ;; and the isaac config path "providers.test.crew" is ""
    ;; when a session "s" is created with explicit crew ""
    ;; then the resolved behavior for "s" has crew "main"
    (pending "not yet implemented"))

  (it "Cascade precedence resolves the right <field> at creation — crew, , , , , alice, alice"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.crew" is "alice"
    ;; and the isaac config path "crew.main.crew" is ""
    ;; and the isaac config path "models.spark.crew" is ""
    ;; and the isaac config path "providers.test.crew" is ""
    ;; when a session "s" is created with explicit crew ""
    ;; then the resolved behavior for "s" has crew "alice"
    (pending "not yet implemented"))

  (it "Cascade precedence resolves the right <field> at creation — crew, bob, , , , alice, bob"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.crew" is "alice"
    ;; and the isaac config path "crew.main.crew" is ""
    ;; and the isaac config path "models.spark.crew" is ""
    ;; and the isaac config path "providers.test.crew" is ""
    ;; when a session "s" is created with explicit crew "bob"
    ;; then the resolved behavior for "s" has crew "bob"
    (pending "not yet implemented"))

  (it "Cascade precedence resolves the right <field> at creation — cwd, , , , , , /test/.isaac/crew/main"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.cwd" is ""
    ;; and the isaac config path "crew.main.cwd" is ""
    ;; and the isaac config path "models.spark.cwd" is ""
    ;; and the isaac config path "providers.test.cwd" is ""
    ;; when a session "s" is created with explicit cwd ""
    ;; then the resolved behavior for "s" has cwd "/test/.isaac/crew/main"
    (pending "not yet implemented"))

  (it "Cascade precedence resolves the right <field> at creation — cwd, /tmp/work, , , , , /tmp/work"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.cwd" is ""
    ;; and the isaac config path "crew.main.cwd" is ""
    ;; and the isaac config path "models.spark.cwd" is ""
    ;; and the isaac config path "providers.test.cwd" is ""
    ;; when a session "s" is created with explicit cwd "/tmp/work"
    ;; then the resolved behavior for "s" has cwd "/tmp/work"
    (pending "not yet implemented"))

  (it "Cascade precedence resolves the right <field> at creation — context-mode, , , , , , :full"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.context-mode" is ""
    ;; and the isaac config path "crew.main.context-mode" is ""
    ;; and the isaac config path "models.spark.context-mode" is ""
    ;; and the isaac config path "providers.test.context-mode" is ""
    ;; when a session "s" is created with explicit context-mode ""
    ;; then the resolved behavior for "s" has context-mode ":full"
    (pending "not yet implemented"))

  (it "Cascade precedence resolves the right <field> at creation — context-mode, , , , , :reset, :reset"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.context-mode" is ":reset"
    ;; and the isaac config path "crew.main.context-mode" is ""
    ;; and the isaac config path "models.spark.context-mode" is ""
    ;; and the isaac config path "providers.test.context-mode" is ""
    ;; when a session "s" is created with explicit context-mode ""
    ;; then the resolved behavior for "s" has context-mode ":reset"
    (pending "not yet implemented"))

  (it "Cascade precedence resolves the right <field> at creation — context-mode, , :reset, , , :full, :reset"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.context-mode" is ":full"
    ;; and the isaac config path "crew.main.context-mode" is ":reset"
    ;; and the isaac config path "models.spark.context-mode" is ""
    ;; and the isaac config path "providers.test.context-mode" is ""
    ;; when a session "s" is created with explicit context-mode ""
    ;; then the resolved behavior for "s" has context-mode ":reset"
    (pending "not yet implemented"))

  (it "Cascade precedence resolves the right <field> at creation — context-mode, :full, :reset, , , :reset, :full"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.context-mode" is ":reset"
    ;; and the isaac config path "crew.main.context-mode" is ":reset"
    ;; and the isaac config path "models.spark.context-mode" is ""
    ;; and the isaac config path "providers.test.context-mode" is ""
    ;; when a session "s" is created with explicit context-mode ":full"
    ;; then the resolved behavior for "s" has context-mode ":full"
    (pending "not yet implemented"))

  (it "Cascade precedence resolves the right <field> at creation — compaction, , , , , , {:strategy :rubberband :threshold 0.8}"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.compaction" is ""
    ;; and the isaac config path "crew.main.compaction" is ""
    ;; and the isaac config path "models.spark.compaction" is ""
    ;; and the isaac config path "providers.test.compaction" is ""
    ;; when a session "s" is created with explicit compaction ""
    ;; then the resolved behavior for "s" has compaction "{:strategy :rubberband :threshold 0.8}"
    (pending "not yet implemented"))

  (it "Cascade precedence resolves the right <field> at creation — compaction, , , , , {:threshold 0.4}, {:strategy :rubberband :threshold 0.4}"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.compaction" is "{:threshold 0.4}"
    ;; and the isaac config path "crew.main.compaction" is ""
    ;; and the isaac config path "models.spark.compaction" is ""
    ;; and the isaac config path "providers.test.compaction" is ""
    ;; when a session "s" is created with explicit compaction ""
    ;; then the resolved behavior for "s" has compaction "{:strategy :rubberband :threshold 0.4}"
    (pending "not yet implemented"))

  (it "Cascade precedence resolves the right <field> at creation — compaction, , , , {:threshold 0.5}, {:threshold 0.4}, {:strategy :rubberband :threshold 0.5}"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.compaction" is "{:threshold 0.4}"
    ;; and the isaac config path "crew.main.compaction" is ""
    ;; and the isaac config path "models.spark.compaction" is ""
    ;; and the isaac config path "providers.test.compaction" is "{:threshold 0.5}"
    ;; when a session "s" is created with explicit compaction ""
    ;; then the resolved behavior for "s" has compaction "{:strategy :rubberband :threshold 0.5}"
    (pending "not yet implemented"))

  (it "Cascade precedence resolves the right <field> at creation — compaction, , , {:threshold 0.6}, {:threshold 0.5}, {:threshold 0.4}, {:strategy :rubberband :threshold 0.6}"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.compaction" is "{:threshold 0.4}"
    ;; and the isaac config path "crew.main.compaction" is ""
    ;; and the isaac config path "models.spark.compaction" is "{:threshold 0.6}"
    ;; and the isaac config path "providers.test.compaction" is "{:threshold 0.5}"
    ;; when a session "s" is created with explicit compaction ""
    ;; then the resolved behavior for "s" has compaction "{:strategy :rubberband :threshold 0.6}"
    (pending "not yet implemented"))

  (it "Cascade precedence resolves the right <field> at creation — compaction, , {:threshold 0.7}, {:threshold 0.6}, {:threshold 0.5}, {:threshold 0.4}, {:strategy :rubberband :threshold 0.7}"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.compaction" is "{:threshold 0.4}"
    ;; and the isaac config path "crew.main.compaction" is "{:threshold 0.7}"
    ;; and the isaac config path "models.spark.compaction" is "{:threshold 0.6}"
    ;; and the isaac config path "providers.test.compaction" is "{:threshold 0.5}"
    ;; when a session "s" is created with explicit compaction ""
    ;; then the resolved behavior for "s" has compaction "{:strategy :rubberband :threshold 0.7}"
    (pending "not yet implemented"))

  (it "Cascade precedence resolves the right <field> at creation — compaction, {:threshold 0.75}, {:threshold 0.7}, {:threshold 0.6}, {:threshold 0.5}, {:threshold 0.4}, {:strategy :rubberband :threshold 0.75}"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.compaction" is "{:threshold 0.4}"
    ;; and the isaac config path "crew.main.compaction" is "{:threshold 0.7}"
    ;; and the isaac config path "models.spark.compaction" is "{:threshold 0.6}"
    ;; and the isaac config path "providers.test.compaction" is "{:threshold 0.5}"
    ;; when a session "s" is created with explicit compaction "{:threshold 0.75}"
    ;; then the resolved behavior for "s" has compaction "{:strategy :rubberband :threshold 0.75}"
    (pending "not yet implemented"))

  (it "Cascade precedence resolves the right <field> at creation — compaction, , {:strategy :slinky}, , , {:threshold 0.4}, {:strategy :slinky :threshold 0.8 :head 0.3 :async? false}"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.compaction" is "{:threshold 0.4}"
    ;; and the isaac config path "crew.main.compaction" is "{:strategy :slinky}"
    ;; and the isaac config path "models.spark.compaction" is ""
    ;; and the isaac config path "providers.test.compaction" is ""
    ;; when a session "s" is created with explicit compaction ""
    ;; then the resolved behavior for "s" has compaction "{:strategy :slinky :threshold 0.8 :head 0.3 :async? false}"
    (pending "not yet implemented"))

  (it "Resolution after config change for <field> — history-retention, :retain, :prune, :retain"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.history-retention" is ":retain"
    ;; when a session "s" is created with explicit history-retention ""
    ;; and the isaac config path "defaults.history-retention" is ":prune"
    ;; then the resolved behavior for "s" has history-retention ":retain"
    (pending "not yet implemented"))

  (it "Resolution after config change for <field> — history-retention, :prune, :retain, :prune"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.history-retention" is ":prune"
    ;; when a session "s" is created with explicit history-retention ""
    ;; and the isaac config path "defaults.history-retention" is ":retain"
    ;; then the resolved behavior for "s" has history-retention ":prune"
    (pending "not yet implemented"))

  (it "Resolution after config change for <field> — crew, alice, bob, alice"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.crew" is "alice"
    ;; when a session "s" is created with explicit crew ""
    ;; and the isaac config path "defaults.crew" is "bob"
    ;; then the resolved behavior for "s" has crew "alice"
    (pending "not yet implemented"))

  (it "Resolution after config change for <field> — effort, 5, 9, 9"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.effort" is "5"
    ;; when a session "s" is created with explicit effort ""
    ;; and the isaac config path "defaults.effort" is "9"
    ;; then the resolved behavior for "s" has effort "9"
    (pending "not yet implemented"))

  (it "Resolution after config change for <field> — effort, 9, 5, 5"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.effort" is "9"
    ;; when a session "s" is created with explicit effort ""
    ;; and the isaac config path "defaults.effort" is "5"
    ;; then the resolved behavior for "s" has effort "5"
    (pending "not yet implemented"))

  (it "Resolution after config change for <field> — model, spark, parrot, parrot"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.model" is "spark"
    ;; when a session "s" is created with explicit model ""
    ;; and the isaac config path "defaults.model" is "parrot"
    ;; then the resolved behavior for "s" has model "parrot"
    (pending "not yet implemented"))

  (it "Resolution after config change for <field> — context-mode, :full, :reset, :reset"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.context-mode" is ":full"
    ;; when a session "s" is created with explicit context-mode ""
    ;; and the isaac config path "defaults.context-mode" is ":reset"
    ;; then the resolved behavior for "s" has context-mode ":reset"
    (pending "not yet implemented"))

  (it "Resolution after config change for <field> — compaction, {:threshold 0.4}, {:threshold 0.5}, {:strategy :rubberband :threshold 0.5}"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.compaction" is "{:threshold 0.4}"
    ;; when a session "s" is created with explicit compaction ""
    ;; and the isaac config path "defaults.compaction" is "{:threshold 0.5}"
    ;; then the resolved behavior for "s" has compaction "{:strategy :rubberband :threshold 0.5}"
    (pending "not yet implemented"))

  (it ":cwd is locked to a session even when the crew default changes"
    ;; given an empty Isaac root at "/test"
    ;; and the isaac config path "defaults.model" is "spark"
    ;; and the isaac config path "providers.test.api" is "grover"
    ;; and the isaac config path "models.spark.model" is "echo"
    ;; and the isaac config path "models.spark.provider" is "test"
    ;; and the isaac config path "models.parrot.model" is "echo"
    ;; and the isaac config path "models.parrot.provider" is "test"
    ;; and the isaac config path "models.swift.model" is "echo"
    ;; and the isaac config path "models.swift.provider" is "test"
    ;; given the isaac config path "defaults.crew" is "alice"
    ;; when a session "s" is created with explicit crew ""
    ;; and the isaac config path "defaults.crew" is "bob"
    ;; then the resolved behavior for "s" has cwd "/test/.isaac/crew/alice"
    (pending "not yet implemented")))
